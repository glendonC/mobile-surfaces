import tokens from "../tokens.json";

export const surfaceColors = tokens.surfaceColors;

export const surfaceSpacing = tokens.surfaceSpacing;

export const surfaceRadii = tokens.surfaceRadii;

export const swiftAssetColorMap = tokens.swiftAssetColorMap;

export type SurfaceColorToken = keyof typeof surfaceColors;
